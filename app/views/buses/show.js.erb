$( () => {$(".trips").hide()
    
    $("#showTrip").on("click", (event)=> {
        event.preventDefault()
        $(".trips").hide()
    });

    $.get("<%=new_student_path%>", data => {$(html).append(data)})

})

